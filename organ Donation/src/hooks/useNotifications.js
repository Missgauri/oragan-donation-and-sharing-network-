/**
 * Re-export hook for convenience — import from hooks instead of context.
 *
 * Usage:
 *   import { useNotifications } from '../hooks/useNotifications';
 */
export { useNotifications } from '../context/NotificationContext';
